{% block body %}

<nav class="navbar navbar-expand-lg rose" data-bs-theme="light">
  <div class="container-fluid container-nowrap">
     <div> {{ render(controller('App\\Controller\\LogoController::logo')) }}</div>
     <div>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link" href="{{path('main')}}">Home
            <span class="visually-hidden">(current)</span>
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{path('app_product')}}">Boutique</a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Categories</a>
          <div class="dropdown-menu">
            {{ render(controller('App\\Controller\\CategoryController::categories')) }}
          </div>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{path('app_register')}}">S'enregistrer</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{path('app_login')}}">Connexion</a>
        </li>
        {% if "ROLE_ADMIN" in app.user.getRoles()|default() %}
          <li class="nav-item">
            <a class="nav-link me-4" href="{{path('admin')}}">Admin</a>
          </li>
        {% endif %}

      <li class="nav-item">
      {% if "ROLE_USER" in app.user.getRoles()|default() %}
        <a class="nav-link" href="{{path('profile_index')}}">
          <span class="material-symbols-outlined">person</span>
        </a>
        {% else %}
        {% endif %}
      </li>

      <li class="nav-item">
        <a class="nav-link me-4" href="{{path('cart_index')}}">
          <span class="material-symbols-outlined">local_mall</span>
        </a>
      </li>
      </ul>

    </div>
    </div>
  </div>
</nav>

{% endblock %}